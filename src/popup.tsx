import ReactDOM from "react-dom/client";

const App = () => {
  return <div className="App">React here!</div>;
};

ReactDOM.createRoot(document.getElementById("root") as HTMLElement).render(
  <App />,
);
